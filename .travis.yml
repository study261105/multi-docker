sudo: required
services:
  - docker

before_install:
  - docker build -t sachin/react-test -f ./client/Dockerfile.dev ./client

script:
  - docker run sachin/react-test npm test -- --coverage

after_success:
  - docker build -t sachin/multi-client ./client
  - docker build -t sachin/multi-nginx ./nginx
  - docker build -t sachin/multi-server ./server
  - docker build -t sachin/multi-worker ./worker
  # Log in to the docker CLI
  - echo "$DOCKER_PASSWORD" | docker login -u "$DOCKER_ID" --password-stdin
  # Take those images and push them to docker hub
  - docker push sachin/multi-client
  - docker push sachin/multi-nginx
  - docker push sachin/multi-server
  - docker push sachin/multi-worker

deploy:
  provider: elasticbeanstalk
  region: us-east-1
  app: multi-docker
  env: MultiDocker-env
  bucket_name: elasticbeanstalk-us-east-1-306476627547
  bucket_path: docker-multi
  on:
    branch: master
  access_key_id: $AWS_ACCESS_KEY
  secret_access_key:
    secure: $AWS_SECRET_KEY
